# Project 08 — Create a Windows Server Virtual Machine

## 🎯 Objective
Install a Windows Server VM and ensure it can communicate with an existing Windows 11 VM on the same network.

## 🧰 Tasks Completed
1. Installed Windows Server VM using:
   - Option #1: VirtualBox (local host) or
   - Option #2: TechLab Cloud Proxmox Server
2. Verified installation by taking a screenshot of the completed installation.
3. Confirmed that Windows 11 VM and Windows Server VM are on the same network.
4. Determined the IP addresses of both VMs.
5. Successfully pinged Windows 11 VM from Windows Server VM.
6. Successfully pinged Windows Server VM from Windows 11 VM.
7. Troubleshot and resolved any ping/connectivity issues.

## 📸 Screenshots
- windows-server-installation.png
- ping-from-windows-11.png
- ping-from-windows-server.png

## 📝 Notes / Highlights
- Ensure both VMs are on the same subnet for successful communication.
- Use the Snipping Tool to capture screenshots of installations and ping tests.
- This project prepares for future networking tasks involving both client and server VMs.

