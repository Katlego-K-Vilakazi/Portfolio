# Project 09 — Create an Active Directory Domain

## 🎯 Objective
Install and configure a Microsoft Server 2022 VM and promote it to a domain controller using Active Directory.

## 🧰 Tasks Completed
1. Installed Windows Server 2022 on a virtual machine.
2. Verified that the VM is functional and has internet access.
3. Determined and recorded the IP address of the server.
4. Installed Active Directory Domain Services (AD DS) role.
5. Promoted the server to a domain controller.
6. Completed the Server 2022 Worksheet, including:
   - Screenshot of `ipconfig` results
   - Screenshot of the Server 2022 desktop/server VM
   - Screenshot of the local server page in Server Manager showing domain controller status
7. Uploaded the completed worksheet to Canvas.

## 📸 Screenshots
- ipconfig-results.png → Screenshot showing IP configuration of the server  
- server-desktop.png → Screenshot of the server VM desktop  
- domain-controller.png → Screenshot of Server Manager showing Active Directory Domain Controller

## 📝 Notes / Highlights
- Ensure static IP is set before promoting to domain controller.
- Use Snipping Tool to capture screenshots of each task.
- Follow Server 2022 Worksheet instructions carefully to ensure all screenshots and steps are included.

